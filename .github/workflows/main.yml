name: PC-V1

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 4680

    steps:
      - name: Configure Core PC-V1 Settings
        shell: powershell
        run: |
          Write-Host "Enabling Remote Desktop for PC-V1 and disabling NLA"
          try {
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
              netsh advfirewall firewall delete rule name="PC-V1-Tailscale" | Out-Null
              netsh advfirewall firewall add rule name="PC-V1-Tailscale" dir=in action=allow protocol=TCP localport=3389
              Restart-Service -Name TermService -Force
              Write-Host "✅ PC-V1 Remote Desktop configuration completed."
          } catch {
              Write-Error "❌ PC-V1 Configuration failed: $($_.Exception.Message)"
              exit 1
          }

          # Power & Timer
          powercfg /change standby-timeout-ac 0
          powercfg /change monitor-timeout-ac 0
          powercfg /hibernate off
          bcdedit /set useplatformclock true
          bcdedit /set tscsyncpolicy Enhanced
          bcdedit /set disabledynamictick yes
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Kernel" /v GlobalTimerResolutionRequests /t REG_DWORD /d 1 /f

          # Network
          netsh int tcp set global autotuninglevel=normal
          netsh int tcp set global chimney=enabled
          netsh int tcp set global ecncapability=enabled
          netsh int tcp set global dca=enabled
          netsh int tcp set global netdma=enabled
          netsh int tcp set global rss=enabled
          netsh int tcp set global pacingprofile=off
          netsh int tcp set global initialRto=300
          netsh int tcp set global rsc=enabled

          # GPU
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\GraphicsDrivers" /v HwSchMode /t REG_DWORD /d 2 /f
          reg add "HKLM\SOFTWARE\Microsoft\DirectX" /v DisableHardwareProtectionFeatures /t REG_DWORD /d 0 /f

          Write-Host "✅ System optimized."

      - name: Enable High Performance Power Plan (Ultimate Fallback)
        shell: powershell
        run: |
          $ultimate = "e9a42b02-d5df-448d-aa00-03f14749eb61"
          $highperf = "8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c"
          $list = powercfg -list | Out-String
          if ($list -match $ultimate) {
              powercfg -setactive $ultimate
              Write-Host "✅ Ultimate Performance activated."
          } else {
              if ($list -notmatch $highperf) { powercfg -restoredefaultschemes }
              powercfg -setactive $highperf
              Write-Host "⚠️ Using High Performance (Ultimate not available)."
          }

      - name: Install DirectStorage 1.3.0 (Official)
        shell: powershell
        run: |
          if (Get-Command winget -ErrorAction SilentlyContinue) {
              & winget install --id Microsoft.Direct3D.DirectStorage --version 1.3.0 --exact --silent --accept-source-agreements --accept-package-agreements
          }
          Write-Host "ℹ️ DirectStorage install attempted (non-critical)."
          exit 0

      - name: Gaming Environment Setup (Safe for GitHub Actions)
        shell: powershell
        run: |
          # Disable Game Bar & DVR
          reg add "HKCU:\Software\Microsoft\GameBar" /v AllowToRunOnDesktop /t REG_DWORD /d 0 /f
          reg add "HKCU:\Software\Microsoft\Windows\CurrentVersion\GameDVR" /v AppCaptureEnabled /t REG_DWORD /d 0 /f
          reg add "HKLM:\SOFTWARE\Policies\Microsoft\Windows\GameDVR" /v AllowGameDVR /t REG_DWORD /d 0 /f
          reg add "HKLM:\SYSTEM\CurrentControlSet\Control\GraphicsDrivers" /v GameDVR_DXGIHonorFSEWindows /t REG_DWORD /d 1 /f

          # Install Steam (non-blocking)
          if (-not (Test-Path "$env:ProgramFiles (x86)\Steam\Steam.exe")) {
              $steam = "$env:TEMP\SteamSetup.exe"
              Invoke-WebRequest "https://cdn.akamai.steamstatic.com/client/installer/SteamSetup.exe" -OutFile $steam
              Start-Process -FilePath $steam -ArgumentList "/S" -Wait
              Remove-Item $steam -ErrorAction SilentlyContinue
          }

          Write-Host "🎮 Gaming environment ready (Steam installed, Game Bar off)."

      - name: GPU & Game Optimizations (NVIDIA/AMD Safe)
        shell: powershell
        run: |
          # NVIDIA
          $nv = "HKLM:\SOFTWARE\NVIDIA Corporation\Global\NVTweak"
          if (Test-Path $nv) {
              New-ItemProperty -Path $nv -Name "PreferMaximumPerformance" -Value 1 -PropertyType DWord -Force
              New-ItemProperty -Path $nv -Name "LowLatencyMode" -Value 1 -PropertyType DWord -Force
          }
          # AMD
          $amd = "HKLM:\SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}\0000"
          if (Test-Path $amd) {
              Set-ItemProperty -Path $amd -Name "AntiLagEnable" -Value 1 -Force
          }
          # Windows Game Config
          $gcs = "HKCU:\Software\Microsoft\GameConfigStore"
          if (-not (Test-Path $gcs)) { New-Item -Path $gcs -Force | Out-Null }
          Set-ItemProperty -Path $gcs -Name "GameDVR_Enabled" -Value 0 -Force
          Set-ItemProperty -Path $gcs -Name "GameDVR_FSEBehavior" -Value 2 -Force
          Write-Host "✅ GPU optimizations applied."

      - name: Create PC-V1 User
        shell: powershell
        run: |
          Add-Type -AssemblyName System.Security
          $chars = ([char[]](65..90) + [char[]](97..122) + [char[]](48..57) + ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126)))
          $pass = -join ($chars | Get-Random -Count 16)
          $sec = ConvertTo-SecureString $pass -AsPlainText -Force
          New-LocalUser -Name "PC-V1" -Password $sec -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "PC-V1"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "PC-V1"
          echo "PCV1_PASSWORD=$pass" >> $env:GITHUB_ENV
          if (-not (Get-LocalUser -Name "PC-V1")) { exit 1 }
          Write-Host "✅ User PC-V1 created."

      - name: Install Tailscale
        shell: powershell
        run: |
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $file = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i", "`"$file`"", "/quiet", "/norestart" -Wait
          Remove-Item $file -Force
          Write-Host "✅ Tailscale installed."

      - name: Establish Tailscale Connection
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=PC-V1
          $ip = $null; $i = 0
          while (-not $ip -and $i -lt 10) {
              $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep 5; $i++
          }
          if (-not $ip) { Write-Error "❌ Tailscale IP not assigned."; exit 1 }
          echo "TAILSCALE_IP=$ip" >> $env:GITHUB_ENV
          Write-Host "✅ Tailscale connected: $ip"

      - name: Verify RDP Connectivity
        shell: powershell
        run: |
          $test = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $test.TcpTestSucceeded) { Write-Error "❌ RDP port 3389 unreachable."; exit 1 }
          Write-Host "✅ RDP connectivity verified."

      - name: Maintain Connection
        shell: powershell
        run: |
          Write-Host "==============================="
          Write-Host "🎮 PC-V1 — Optimized for GTA V / FiveM / Steam"
          Write-Host "🎯 Target: Stable 90 FPS"
          Write-Host "🌐 Address: $env:TAILSCALE_IP"
          Write-Host "👤 Username: PC-V1"
          Write-Host "🔑 Password: $env:PCV1_PASSWORD"
          Write-Host "==============================="
          while ($true) {
              Write-Host "[$(Get-Date)] PC-V1 Session Active — Cancel workflow to stop"
              Start-Sleep -Seconds 300
          }
