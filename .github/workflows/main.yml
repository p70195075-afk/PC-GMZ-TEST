name: PC-V1

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 4680

    steps:
      - name: Configure Core PC-V1 Settings
        shell: powershell
        run: |
          Write-Host "Enabling Remote Desktop for PC-V1 and disabling NLA"
          try {
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0 -Force
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 0 -Force
              netsh advfirewall firewall delete rule name="PC-V1-Tailscale" | Out-Null
              netsh advfirewall firewall add rule name="PC-V1-Tailscale" dir=in action=allow protocol=TCP localport=3389
              Restart-Service -Name TermService -Force
              Write-Host "PC-V1 Remote Desktop configuration completed successfully."
          }
          catch {
              Write-Error "PC-V1 Configuration failed: $($_.Exception.Message)"
              exit 1
          }

          # Power & System Optimization
          powercfg /change standby-timeout-ac 0
          powercfg /change monitor-timeout-ac 0
          powercfg /hibernate off

          # High-Precision & Low-Latency Timer
          bcdedit /set useplatformclock true
          bcdedit /set tscsyncpolicy Enhanced
          bcdedit /set disabledynamictick yes
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Kernel" /v GlobalTimerResolutionRequests /t REG_DWORD /d 1 /f

          # Network Optimizations
          netsh int tcp set global autotuninglevel=normal
          netsh int tcp set global chimney=enabled
          netsh int tcp set global ecncapability=enabled
          netsh int tcp set global dca=enabled
          netsh int tcp set global netdma=enabled
          netsh int tcp set global rss=enabled
          netsh int tcp set global pacingprofile=off
          netsh int tcp set global initialRto=300
          netsh int tcp set global rsc=enabled

          # GPU & Graphics
          reg add "HKLM\SYSTEM\CurrentControlSet\Control\GraphicsDrivers" /v HwSchMode /t REG_DWORD /d 2 /f  # Hardware-Accelerated GPU Scheduling
          reg add "HKLM\SOFTWARE\Microsoft\DirectX" /v DisableHardwareProtectionFeatures /t REG_DWORD /d 0 /f

          Write-Host "System optimization applied successfully."

      - name: Enable Ultimate Performance Power Plan
        shell: powershell
        run: |
          powercfg -duplicatescheme e9a42b02-d5df-448d-aa00-03f14749eb61
          powercfg -setactive e9a42b02-d5df-448d-aa00-03f14749eb61
          Write-Host "Ultimate Performance power plan activated."

      - name: Install & Configure Steam + GTA V + FiveM
        shell: powershell
        run: |
          # Install Steam silently
          $steamUrl = "https://cdn.akamai.steamstatic.com/client/installer/SteamSetup.exe"
          $steamPath = "$env:TEMP\SteamSetup.exe"
          Invoke-WebRequest -Uri $steamUrl -OutFile $steamPath
          Start-Process -FilePath $steamPath -ArgumentList "/S" -Wait
          Remove-Item $steamPath -Force

          # Enable Steam auto-login (optional, manual setup later)
          New-Item -Path "HKCU:\Software\Valve\Steam" -Force | Out-Null
          Set-ItemProperty -Path "HKCU:\Software\Valve\Steam" -Name "AutoLoginUser" -Value "steamuser" -Force
          Set-ItemProperty -Path "HKCU:\Software\Valve\Steam" -Name "RememberPassword" -Value 1 -Force

          # Enable Game Mode & Disable Game Bar
          reg add "HKCU:\Software\Microsoft\GameBar" /v AllowToRunOnDesktop /t REG_DWORD /d 0 /f
          reg add "HKCU:\Software\Microsoft\Windows\CurrentVersion\GameDVR" /v AppCaptureEnabled /t REG_DWORD /d 0 /f
          reg add "HKLM:\SOFTWARE\Policies\Microsoft\Windows\GameDVR" /v AllowGameDVR /t REG_DWORD /d 0 /f
          reg add "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\GameDVR" /v GameDVR_Enabled /t REG_DWORD /d 0 /f
          reg add "HKLM:\SYSTEM\CurrentControlSet\Control\GraphicsDrivers" /v GameDVR_DXGIHonorFSEWindows /t REG_DWORD /d 1 /f

          # Install DirectStorage (for GPU decompression in future games)
          if (Get-Command winget -ErrorAction SilentlyContinue) {
            try {
              & winget install --id Microsoft.DirectStorage -e --accept-source-agreements --accept-package-agreements --scope machine
              Write-Host "DirectStorage installed."
            } catch {
              Write-Host "DirectStorage install skipped."
            }
          }

          # Optimize Virtual Memory for Gaming
          $pageFile = Get-WmiObject -Class Win32_PageFileSetting
          if ($pageFile) {
            $pageFile.Delete()
          }
          $sys = Get-WmiObject -Class Win32_ComputerSystem
          $sys.AutomaticManagedPagefile = $false
          $sys.Put()
          $page = ([wmiclass]"Win32_PageFileSetting").CreateInstance()
          $page.Name = "C:\pagefile.sys"
          $page.InitialSize = 8192  # 8 GB
          $page.MaximumSize = 16384 # 16 GB
          $page.Put()

          Write-Host "Steam, DirectStorage, and gaming environment configured."

      - name: GPU-Specific Gaming Optimizations (NVIDIA/AMD)
        shell: powershell
        run: |
          # NVIDIA: Enable low-latency mode + performance preference
          $nvidiaPath = "HKLM:\SOFTWARE\NVIDIA Corporation\Global\NVTweak"
          if (Test-Path $nvidiaPath) {
            New-ItemProperty -Path $nvidiaPath -Name "OptimusMaxRenderClock" -Value 1 -PropertyType DWord -Force
            New-ItemProperty -Path $nvidiaPath -Name "PreferMaximumPerformance" -Value 1 -PropertyType DWord -Force
            New-ItemProperty -Path $nvidiaPath -Name "LowLatencyMode" -Value 1 -PropertyType DWord -Force
          }

          # AMD: Enable Radeon Anti-Lag & Boost (if detected)
          $amdPath = "HKLM:\SYSTEM\CurrentControlSet\Control\Class\{4d36e968-e325-11ce-bfc1-08002be10318}"
          if (Test-Path "$amdPath\0000") {
            Set-ItemProperty -Path "$amdPath\0000" -Name "PP_DynamicRefreshrate" -Value 1 -Force
            Set-ItemProperty -Path "$amdPath\0000" -Name "AntiLagEnable" -Value 1 -Force
          }

          # Windows Game Config Store
          $gcs = "HKCU:\Software\Microsoft\GameConfigStore"
          if (-not (Test-Path $gcs)) { New-Item -Path $gcs -Force | Out-Null }
          Set-ItemProperty -Path $gcs -Name "GameDVR_Enabled" -Value 0 -Force
          Set-ItemProperty -Path $gcs -Name "GameDVR_FSEBehavior" -Value 2 -Force

          Write-Host "GPU-specific gaming optimizations applied."

      - name: Create PC-V1 User with Secure Password
        shell: powershell
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) + [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "PC-V1" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "PC-V1"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "PC-V1"
          echo "PCV1_PASSWORD=$password" >> $env:GITHUB_ENV

          if (-not (Get-LocalUser -Name "PC-V1")) {
              Write-Error "User creation failed"
              exit 1
          }

      - name: Install Tailscale
        shell: powershell
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=PC-V1
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify PC-V1 Accessibility
        shell: powershell
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to port 3389 failed"
              exit 1
          }
          Write-Host "PC-V1 Remote Desktop connectivity successful!"

      - name: Maintain Connection
        shell: powershell
        run: |
          Write-Host "==================="
          Write-Host "🎮 PC-V1 Gaming Optimized for Steam / GTA V / FiveM"
          Write-Host "🎯 Target: 90 FPS Stable"
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: PC-V1"
          Write-Host "Password: $env:PCV1_PASSWORD"
          Write-Host "==================="
          while ($true) {
              Write-Host "[$(Get-Date)] PC-V1 Gaming Session Active - Use Ctrl+C or cancel workflow to terminate"
              Start-Sleep -Seconds 300
          }