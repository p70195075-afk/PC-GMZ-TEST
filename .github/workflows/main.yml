name: PC-V1

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 4680

    steps:
      - name: Disable Sleep / Hibernate / PowerSave
        shell: powershell
        run: |
          powercfg /change standby-timeout-ac 0
          powercfg /change monitor-timeout-ac 0
          powercfg /hibernate off
          Write-Host "✅ Power Save Disabled"

      - name: System Performance Optimizations
        shell: powershell
        run: |
          Write-Host "🚀 Applying latency & performance tweaks..."
          netsh int tcp set global autotuninglevel=highlyrestricted
          netsh int tcp set global rss=enabled
          netsh int tcp set global chimney=enabled
          netsh int tcp set global dca=enabled
          netsh int tcp set supplemental template=internet congestionprovider=bbr2
          
          reg add "HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" /v TcpAckFrequency /t REG_DWORD /d 1 /f
          reg add "HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" /v TCPNoDelay /t REG_DWORD /d 1 /f

          bcdedit /set useplatformclock true
          bcdedit /set tscsyncpolicy Enhanced
          bcdedit /set disabledynamictick yes

          reg add "HKLM\SOFTWARE\Microsoft\DirectX" /v MaxFrameLatency /t REG_DWORD /d 1 /f
          Write-Host "✅ System optimizations applied!"

      - name: Create PC-V1 User with Secure Password
        shell: powershell
        run: |
          Write-Host "👤 Creating PC-V1 user account..."
          
          # Generate simple password yang pasti work
          $password = "P@ssw0rd123!"  # Password fix yang work
          
          # Delete user lama kalo ada
          net user PC-V1 /delete 2>&1 | Out-Null
          Start-Sleep -Seconds 2
          
          # Buat user baru pake command line (paling reliable)
          net user PC-V1 $password /add /Y
          net localgroup administrators PC-V1 /add
          net localgroup "Remote Desktop Users" PC-V1 /add
          
          echo "PCV1_PASSWORD=$password" >> $env:GITHUB_ENV
          
          Write-Host "=== PC-V1 ACCESS ==="
          Write-Host "Username: PC-V1"
          Write-Host "Password: $password"
          Write-Host "===================="

      - name: Enable Remote Desktop
        shell: powershell
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name "TermService" -Force
          Write-Host "✅ Remote Desktop enabled."

      - name: Install Tailscale
        shell: powershell
        run: |
          Write-Host "🌐 Installing Tailscale..."
          $url = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $file = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $url -OutFile $file
          Start-Process msiexec.exe -ArgumentList "/i", "`"$file`"", "/quiet", "/norestart" -Wait
          Remove-Item $file -Force
          Write-Host "✅ Tailscale installed successfully."

      - name: Connect to Tailscale
        shell: powershell
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=PC-V1 --accept-routes --accept-dns=false
          Start-Sleep -Seconds 10
          $tsIP = (& "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4)
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "✅ Connected to Tailscale - IP: $tsIP"

      - name: Setup Sunshine Ports
        shell: powershell
        run: |
          Write-Host "🔧 Setting up Sunshine ports..."
          $ports = @(47984, 47989, 47990, 48010)
          foreach ($port in $ports) {
              netsh advfirewall firewall add rule name="Sunshine-TCP-$port" dir=in action=allow protocol=TCP localport=$port 2>&1 | Out-Null
              netsh advfirewall firewall add rule name="Sunshine-UDP-$port" dir=in action=allow protocol=UDP localport=$port 2>&1 | Out-Null
          }
          Write-Host "✅ Sunshine ports configured"

      - name: Download and Install Sunshine (SIMPLE VERSION)
        shell: powershell
        run: |
          Write-Host "🌞 DOWNLOADING SUNSHINE..."
          
          # Cuma pake 1 source yang paling reliable
          $url = "https://github.com/LizardByte/Sunshine/releases/latest/download/Sunshine-Windows.exe"
          $installer = "$env:TEMP\Sunshine.exe"
          
          try {
              # Download
              Invoke-WebRequest -Uri $url -OutFile $installer -TimeoutSec 60
              Write-Host "✅ Download successful"
              
              # Install pake method yang simple
              Write-Host "🔄 Installing Sunshine..."
              Start-Process -FilePath $installer -ArgumentList "--silent" -Wait -NoNewWindow
              Write-Host "✅ Installation completed"
              
              # Tunggu sebentar
              Start-Sleep -Seconds 15
              
              # Coba start service
              Start-Service -Name "Sunshine" -ErrorAction SilentlyContinue
              Write-Host "✅ Service started"
              
          } catch {
              Write-Host "⚠️ Sunshine installation had issues but continuing..."
          }
          
          # Cleanup
          Remove-Item $installer -Force -ErrorAction SilentlyContinue

      - name: Final Setup and Info
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "🎉🎉🎉 PC-V1 SUDAH SIAP BRO! 🎉🎉🎉"
          Write-Host "========================================"
          Write-Host "🌐 TAILSCALE IP: $env:TAILSCALE_IP"
          Write-Host "👤 USERNAME: PC-V1"
          Write-Host "🔑 PASSWORD: $env:PCV1_PASSWORD"
          Write-Host ""
          Write-Host "🚀 CARA PAKE:"
          Write-Host "1. Buka REMOTE DESKTOP di Windows"
          Write-Host "2. Masukin IP: $env:TAILSCALE_IP"
          Write-Host "3. Username: PC-V1"
          Write-Host "4. Password: $env:PCV1_PASSWORD"
          Write-Host ""
          Write-Host "🎮 UNTUK MOONLIGHT:"
          Write-Host "Buka Moonlight > Add PC > https://$env:TAILSCALE_IP:47990"
          Write-Host ""
          Write-Host "⏰ SESSION AKAN TETAP HIDUP SELAMA 3 HARI"
          Write-Host "========================================"

      - name: Keep Alive Forever
        shell: powershell
        run: |
          Write-Host "🔄 KEEPING PC-V1 ALIVE..."
          $counter = 0
          while ($true) {
              $counter++
              Write-Host "[$(Get-Date)] PC-V1 MASIH NYALA - $counter minutes - IP: $env:TAILSCALE_IP"
              Write-Host "🚀 BUAT MAIN GAME, PAKE RDP DULU UNTUK SETUP SUNSHINE MANUAL"
              Write-Host "💡 Kalo Sunshine gak jalan, install manual via RDP aja"
              Start-Sleep -Seconds 60
          }