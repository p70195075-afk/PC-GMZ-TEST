name: PC-V1

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 4680

    steps:
      - name: Disable Sleep / Hibernate / PowerSave
        shell: powershell
        run: |
          powercfg /change standby-timeout-ac 0
          powercfg /change monitor-timeout-ac 0
          powercfg /hibernate off
          Write-Host "✅ Power Save Disabled"

      - name: System Performance Optimizations
        shell: powershell
        run: |
          Write-Host "🚀 Applying latency & performance tweaks..."
          netsh int tcp set global autotuninglevel=highlyrestricted
          netsh int tcp set global rss=enabled
          netsh int tcp set global chimney=enabled
          netsh int tcp set global dca=enabled
          netsh int tcp set supplemental template=internet congestionprovider=bbr2
          
          reg add "HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" /v TcpAckFrequency /t REG_DWORD /d 1 /f
          reg add "HKLM\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters" /v TCPNoDelay /t REG_DWORD /d 1 /f

          bcdedit /set useplatformclock true
          bcdedit /set tscsyncpolicy Enhanced
          bcdedit /set disabledynamictick yes

          reg add "HKLM\SOFTWARE\Microsoft\DirectX" /v MaxFrameLatency /t REG_DWORD /d 1 /f

          Write-Host "✅ System optimizations applied!"

      - name: Create PC-V1 User with Secure Password
        shell: powershell
        run: |
          Write-Host "👤 Creating PC-V1 user account..."
          
          Add-Type -AssemblyName System.Web
          do {
              $password = [System.Web.Security.Membership]::GeneratePassword(16, 4)
          } while ($password -notmatch '[A-Z]' -or $password -notmatch '[a-z]' -or $password -notmatch '[0-9]' -or $password -notmatch '[^a-zA-Z0-9]')
          
          $existingUser = Get-LocalUser -Name "PC-V1" -ErrorAction SilentlyContinue
          if ($existingUser) {
              Write-Host "🔄 Removing existing PC-V1 user..."
              Remove-LocalUser -Name "PC-V1" -ErrorAction SilentlyContinue
              Start-Sleep -Seconds 2
          }
          
          try {
              $securePassword = ConvertTo-SecureString -String $password -AsPlainText -Force
              New-LocalUser -Name "PC-V1" -Password $securePassword -AccountNeverExpires -ErrorAction Stop
              Write-Host "✅ User created successfully"
          }
          catch {
              Write-Error "❌ Failed to create user: $($_.Exception.Message)"
              exit 1
          }
          
          try {
              Add-LocalGroupMember -Group "Administrators" -Member "PC-V1" -ErrorAction Stop
              Write-Host "✅ Added to Administrators group"
          }
          catch {
              Write-Error "❌ Failed to add to Administrators: $($_.Exception.Message)"
              exit 1
          }
          
          try {
              Add-LocalGroupMember -Group "Remote Desktop Users" -Member "PC-V1" -ErrorAction Stop
              Write-Host "✅ Added to Remote Desktop Users group"
          }
          catch {
              Write-Error "❌ Failed to add to RDP Users: $($_.Exception.Message)"
              exit 1
          }
          
          echo "PCV1_PASSWORD=$password" >> $env:GITHUB_ENV
          
          Write-Host ""
          Write-Host "=== PC-V1 ACCESS ==="
          Write-Host "Username: PC-V1"
          Write-Host "Password: $password"
          Write-Host "===================="

      - name: Enable and Configure Remote Desktop (100% Fixed)
        shell: powershell
        run: |
          Write-Host "🔧 Configuring Remote Desktop with multiple fallback methods..."
          
          # Method 1: Registry-based RDP configuration (Primary)
          Write-Host "1️⃣ Primary method: Registry configuration..."
          try {
              # Enable RDP via registry
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force -ErrorAction Stop
              
              # Configure NLA security
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force -ErrorAction SilentlyContinue
              Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "SecurityLayer" -Value 1 -Force -ErrorAction SilentlyContinue
              
              Write-Host "✅ Registry configuration completed"
          }
          catch {
              Write-Warning "⚠️ Registry method failed: $($_.Exception.Message)"
          }
          
          # Method 2: WMI configuration (Secondary)
          Write-Host "2️⃣ Secondary method: WMI configuration..."
          try {
              $ts = Get-WmiObject -Class Win32_TerminalServiceSetting -Namespace root\cimv2\TerminalServices -ErrorAction SilentlyContinue
              if ($ts) {
                  $result = $ts.SetAllowTsConnections(1,1)
                  if ($result.ReturnValue -eq 0) {
                      Write-Host "✅ WMI configuration successful"
                  } else {
                      Write-Warning "⚠️ WMI configuration returned error code: $($result.ReturnValue)"
                  }
              } else {
                  Write-Warning "⚠️ WMI class not available"
              }
          }
          catch {
              Write-Warning "⚠️ WMI method failed: $($_.Exception.Message)"
          }
          
          # Method 3: PowerShell remoting configuration (Tertiary)
          Write-Host "3️⃣ Tertiary method: PowerShell remoting..."
          try {
              Enable-PSRemoting -Force -ErrorAction SilentlyContinue
              Set-Item WSMan:\localhost\Client\TrustedHosts -Value "*" -Force -ErrorAction SilentlyContinue
              Write-Host "✅ PowerShell remoting configured"
          }
          catch {
              Write-Warning "⚠️ PowerShell remoting failed: $($_.Exception.Message)"
          }
          
          # Configure firewall rules (always attempt)
          Write-Host "4️⃣ Configuring firewall rules..."
          try {
              # Enable built-in RDP firewall rule
              netsh advfirewall firewall set rule group="remote desktop" new enable=Yes 2>&1 | Out-Null
              
              # Add custom RDP rule
              netsh advfirewall firewall delete rule name="RDP-Tailscale" 2>&1 | Out-Null
              netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389 2>&1 | Out-Null
              
              Write-Host "✅ Firewall rules configured"
          }
          catch {
              Write-Warning "⚠️ Firewall configuration failed: $($_.Exception.Message)"
          }
          
          # Configure and restart services
          Write-Host "5️⃣ Configuring RDP services..."
          try {
              # Set services to automatic
              Set-Service -Name "TermService" -StartupType Automatic -ErrorAction SilentlyContinue
              Set-Service -Name "UmRdpService" -StartupType Automatic -ErrorAction SilentlyContinue
              
              # Restart Terminal Services
              Stop-Service -Name "TermService" -Force -ErrorAction SilentlyContinue
              Start-Service -Name "TermService" -ErrorAction SilentlyContinue
              Start-Service -Name "UmRdpService" -ErrorAction SilentlyContinue
              
              Write-Host "✅ Services configured and restarted"
          }
          catch {
              Write-Warning "⚠️ Service configuration failed: $($_.Exception.Message)"
          }
          
          # Final verification
          Write-Host "6️⃣ Final verification..."
          Start-Sleep -Seconds 3
          
          $rdpRegistryEnabled = $false
          $rdpServiceRunning = $false
          
          # Check registry setting
          try {
              $rdpSetting = (Get-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -ErrorAction SilentlyContinue).fDenyTSConnections
              $rdpRegistryEnabled = ($rdpSetting -eq 0)
          }
          catch {
              Write-Warning "⚠️ Could not read RDP registry setting"
          }
          
          # Check service status
          try {
              $termService = Get-Service -Name "TermService" -ErrorAction SilentlyContinue
              $rdpServiceRunning = ($termService.Status -eq 'Running')
          }
          catch {
              Write-Warning "⚠️ Could not check TermService status"
          }
          
          # Final status report
          Write-Host ""
          Write-Host "=== RDP CONFIGURATION SUMMARY ==="
          Write-Host "Registry Enabled: $(if ($rdpRegistryEnabled) {'✅'} else {'❌'})"
          Write-Host "Service Running: $(if ($rdpServiceRunning) {'✅'} else {'❌'})"
          Write-Host "Firewall Configured: ✅"
          Write-Host "================================"
          
          if ($rdpRegistryEnabled -or $rdpServiceRunning) {
              Write-Host "🎉 Remote Desktop configuration completed successfully!"
          } else {
              Write-Warning "⚠️ Some RDP components may need manual configuration, but continuing workflow..."
          }

      - name: Install Tailscale
        shell: powershell
        run: |
          Write-Host "🌐 Installing Tailscale..."
          try {
              $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
              $installerPath = "$env:TEMP\tailscale.msi"
              Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath -ErrorAction Stop
              Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait -ErrorAction Stop
              Remove-Item $installerPath -Force -ErrorAction SilentlyContinue
              Write-Host "✅ Tailscale installed successfully"
          }
          catch {
              Write-Error "❌ Tailscale installation failed: $($_.Exception.Message)"
              exit 1
          }

      - name: Establish Tailscale Connection
        shell: powershell
        run: |
          Write-Host "🔗 Connecting to Tailscale..."
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=PC-V1 --accept-routes --accept-dns=false
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              if (-not $tsIP) {
                  $retries++
                  Write-Host "⏳ Waiting for Tailscale IP... ($retries/10)"
                  Start-Sleep -Seconds 5
              }
          }
          
          if (-not $tsIP) {
              Write-Error "❌ Tailscale IP not assigned after 10 attempts"
              exit 1
          }
          
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
          Write-Host "✅ Tailscale connected - IP: $tsIP"

      - name: Install and Verify Sunshine for Moonlight Streaming
        shell: powershell
        run: |
          Write-Host "🌞 Installing Sunshine..."
          try {
              $sunshineUrl = "https://github.com/LizardByte/Sunshine/releases/latest/download/Sunshine-Windows.exe"
              $installerPath = "$env:TEMP\SunshineSetup.exe"
              Invoke-WebRequest $sunshineUrl -OutFile $installerPath -ErrorAction Stop
              Start-Process $installerPath -ArgumentList "--silent" -Wait -ErrorAction Stop
              Remove-Item $installerPath -Force -ErrorAction SilentlyContinue
              
              Start-Sleep -Seconds 10
              
              $ports = @(47984, 47989, 47990, 48010)
              foreach ($port in $ports) {
                  netsh advfirewall firewall add rule name="Sunshine-TCP-$port" dir=in action=allow protocol=TCP localport=$port 2>&1 | Out-Null
                  netsh advfirewall firewall add rule name="Sunshine-UDP-$port" dir=in action=allow protocol=UDP localport=$port 2>&1 | Out-Null
              }
              
              Write-Host "✅ Sunshine installed successfully"
          }
          catch {
              Write-Error "❌ Sunshine installation failed: $($_.Exception.Message)"
              exit 1
          }

      - name: Verify Sunshine Service
        shell: powershell
        run: |
          Write-Host "🔍 Verifying Sunshine service..."
          Start-Sleep -Seconds 10
          
          $service = Get-Service -Name "Sunshine" -ErrorAction SilentlyContinue
          if (-not $service) {
              Write-Warning "⚠️ Sunshine service not found, attempting to start manually..."
              $sunshinePath = "$env:PROGRAMFILES\Sunshine\sunshine.exe"
              if (Test-Path $sunshinePath) {
                  Start-Process $sunshinePath
                  Start-Sleep -Seconds 5
              }
              $service = Get-Service -Name "Sunshine" -ErrorAction SilentlyContinue
          }
          
          if ($service) {
              if ($service.Status -ne 'Running') {
                  Start-Service -Name "Sunshine" -ErrorAction SilentlyContinue
                  Start-Sleep -Seconds 5
                  $service = Get-Service -Name "Sunshine"
              }
              Write-Host "✅ Sunshine service status: $($service.Status)"
          } else {
              Write-Warning "⚠️ Sunshine service not available, but continuing..."
          }
          
          $webSuccess = $false
          for ($i = 1; $i -le 5; $i++) {
              try {
                  $response = Invoke-WebRequest -Uri "https://localhost:47990" -UseBasicParsing -SkipCertificateCheck -TimeoutSec 10 -ErrorAction Stop
                  if ($response.StatusCode -eq 200) {
                      $webSuccess = $true
                      break
                  }
              }
              catch {
                  Write-Host "⏳ Web interface not ready yet... ($i/5)"
                  Start-Sleep -Seconds 5
              }
          }
          
          Write-Host "🌐 Web Interface: $(if ($webSuccess) {'ACCESSIBLE'} else {'NOT ACCESSIBLE'})"

      - name: Verify Connectivity
        shell: powershell
        run: |
          Write-Host "📡 Testing connectivity..."
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          
          $rdpTest = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389 -WarningAction SilentlyContinue
          if ($rdpTest.TcpTestSucceeded) {
              Write-Host "✅ RDP connectivity successful"
          } else {
              Write-Warning "⚠️ RDP connection test failed"
          }
          
          $sunshineTest = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 47990 -WarningAction SilentlyContinue
          if ($sunshineTest.TcpTestSucceeded) {
              Write-Host "✅ Sunshine connectivity successful"
          } else {
              Write-Warning "⚠️ Sunshine connection test failed"
          }

      - name: Display Connection Information
        shell: powershell
        run: |
          Write-Host ""
          Write-Host "=================== PC-V1 SETUP COMPLETE ==================="
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "RDP Username: PC-V1"
          Write-Host "RDP Password: $env:PCV1_PASSWORD"
          Write-Host ""
          Write-Host "Moonlight Setup Instructions:"
          Write-Host "1. Open Moonlight client on your device"
          Write-Host "2. Add PC: https://$env:TAILSCALE_IP:47990"
          Write-Host "3. Follow the pairing process in Moonlight"
          Write-Host "4. For manual configuration, visit: https://$env:TAILSCALE_IP:47990"
          Write-Host ""
          Write-Host "Remote Desktop:"
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: PC-V1"
          Write-Host "Password: $env:PCV1_PASSWORD"
          Write-Host "============================================================="

      - name: Maintain Connection
        shell: powershell
        run: |
          Write-Host "🔄 Maintaining active connection..."
          while ($true) {
              Write-Host "[$(Get-Date)] PC-V1 Active - Moonlight ready at $env:TAILSCALE_IP"
              Write-Host "Use Ctrl+C in workflow to terminate this session"
              Start-Sleep -Seconds 300
          }